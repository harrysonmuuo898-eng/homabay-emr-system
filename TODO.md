# TODO

## Task: Add all departments to homepage + fix script.js error

- [ ] Fix duplicated functions in `hospital-emr-frontend/script.js` (remove extra `createStaffFromForm` and `fetchAndDisplayStaff` definitions).
- [ ] Update `hospital-emr-frontend/index.html` to include a container for department cards (e.g., `#departmentGrid`).
- [ ] Update `hospital-emr-frontend/script.js` to render **all departments** into `#departmentGrid` using backend list when possible, otherwise fallback mapping to existing department pages.
- [ ] Verify navigation links work for each rendered department page.
- [ ] Smoke test in browser: open `hospital-emr-frontend/index.html` and ensure no JS errors.

